# Custom OpenAI-compatible backend

Set backend base URL and auth in Swobu, then route OpenAI-style request families through the local endpoint.

Start with `/v1/chat/completions` for initial verification, then expand to other families.
