# Anthropic-compatible clients

Use Swobu's Anthropic surface (`/v1/messages`) and point client base URL to Swobu local endpoint.

Keep client-side variables aligned to your Anthropic-compatible tool requirements, and configure backend auth in Swobu.
