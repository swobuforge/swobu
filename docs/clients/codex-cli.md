# Codex CLI

1. Run `swobu` and configure a route.
2. Set Codex CLI base URL to Swobu local endpoint.
3. Keep client auth values as required by Codex CLI, and backend auth in Swobu.

If routing appears correct but responses fail, capture request family and backend error payload for compatibility debugging.
