// Package livematrix captures low-cost live provider compatibility scenario cases.
//
// It supports both direct provider-edge capture and session-locked Swobu-path
// capture (client -> Swobu -> provider), then persists deterministic replay
// artifacts for offline adapter verification.
package livematrix
