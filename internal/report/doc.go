// Package report owns the shared exchange-report vocabulary used by
// conformance replay, exchange execution, and operator surfaces.
//
// It carries exchange-level report shapes, losses, notices, and evidence in a
// single package so transform execution can stay on the outcome side of the
// boundary instead of introducing another report namespace.
package report
