// Package run_once owns the inline smoke-test disclosure workflow.
//
// The workflow shows the selected route/model and adapter-provided command
// preview before executing through the RunExecutor port. It is intentionally a
// narrow smoke-test affordance, not a generic client workbench.
package run_once
