# qrcode-generator

`qrcode.min.js` is the browser distributable of Kazuhiko Arase's
`qrcode-generator` 1.4.4, retrieved from cdnjs. It is vendored so Share QR
generation stays local, deterministic, and free of runtime dependencies or
secret-bearing network requests. The adjacent `LICENSE` contains the MIT terms.
