// Package routing owns the cockpit routing section feature composites.
//
// Axes:
//   - Feature: routing (execution choice, run-on selection)
//   - Layer: feature composites
//   - Concern: view rendering, view state, intent emission
//
// This package owns routing section, run-on rows, provider panels, property
// rows, and pure formatting. It does not own mode decisions (shell/) or state
// (state/).
package routing
