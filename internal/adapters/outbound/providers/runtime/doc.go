// Package runtime owns provider-facet role contracts, runtime bundle wiring,
// and runtime-factory registration for the outbound provider namespace.
//
// It defines the live provider execution and discovery roles used by the
// provider registry, while provider packages own the concrete implementations
// and register runtime factories at the namespace edge.
package runtime
