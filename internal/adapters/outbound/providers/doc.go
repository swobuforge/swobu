// Package providers owns outbound provider registry and dispatch.
// It selects one provider-owned adapter bundle per configured provider id at
// the composition edge and keeps route-support policy in providercompat.
package providers
