// Package together binds Together AI's one Chat Completions inference API to
// the shared OpenAI-family transport. It owns only Together's two-source
// operator catalog and documented reasoning dialect; deployment and model
// capability policy remain outside the request path.
package together
