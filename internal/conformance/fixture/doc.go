// Package fixture defines exchange snapshot fixture layout contracts.
//
// The replay corpus uses upstream_* names for the provider-facing leg so the
// fixture paths stay directional and do not collapse provider truth into a
// generic provider_* alias.
package fixture
